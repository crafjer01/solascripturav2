
export const getRepositoryQuestions = (books = []) => {
    if ( books.length != 0 ) {
        books.forEach(book => {
            //preguntasPorLibro = juego.repositorioPreguntas.filter( p => p.libro == l);
            // preguntas = preguntas.concat(preguntasPorLibro);

        });
    } 

}
